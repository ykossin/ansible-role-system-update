# Изменения в роли system-update

**Дата:** январь 2026  
**Автор:** y.kossin

---

## ✅ Выполненные изменения

### 1. Автор изменен на `y.kossin`

**Файл:** `meta/main.yml`
```yaml
galaxy_info:
  author: y.kossin
  role_name: system-update
```

### 2. Убраны все упоминания старого автора

- ✅ Удалено из `meta/main.yml` (company)
- ✅ Обновлены все документационные файлы

### 3. Образы настроены на Docker Hub

**Файл:** `molecule/default/molecule.yml`

Все образы используют `ykossin/docker-systemd` из Docker Hub:
- ✅ `ykossin/docker-systemd:debian-bookworm`
- ✅ `ykossin/docker-systemd:debian-bullseye`
- ✅ `ykossin/docker-systemd:ubuntu-22.04`
- ✅ `ykossin/docker-systemd:ubuntu-20.04`

### 4. Dependency отключен для локальной роли

**Файл:** `molecule/default/molecule.yml`
```yaml
dependency:
  name: galaxy
  enabled: false  # Используем локальную роль
```

### 5. Создан `.ansible-lint` для пропуска проверки

**Файл:** `.ansible-lint`
```yaml
skip_list:
  - role-name  # Пропустить проверку имени роли
```

---

## 📋 Проверка изменений

### Проверка автора
```bash
grep "author" meta/main.yml
# Результат: author: y.kossin
```

### Проверка образов
```bash
grep "image:" molecule/default/molecule.yml
# Все образы: ykossin/docker-systemd:*
```

### Проверка упоминаний старого автора
```bash
grep -ri "old_author" .
# Не должно быть результатов
```

---

## ⚠️ Известная проблема

Molecule 25.x проверяет namespace Galaxy даже для локальных ролей через ansible-compat.

**Решение:** Использовать ansible-playbook напрямую или обновить Molecule до версии, которая поддерживает локальные роли без проверки namespace.

---

**Статус:** Все изменения применены, конфигурация готова к использованию
